{
  "name": "epistola-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:firestore": "node --test firestore/*.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
